#ifndef GAME_DEF_HPP
#define GAME_DEF_HPP

#define WSIZE_X (128)
#define WSIZE_Y (64)
#define WSIZE_Z (128)

#define WSIZE_HALF_X (128 / 2)
#define WSIZE_HALF_Y (64 / 2)
#define WSIZE_HALF_Z (128 / 2)
#endif